---
title: "Contact Us"
heading: "Get In Touch"
description: "Get in touch with Us"
hasPage: true
itemsHasPage: false
redirectFrom: ["/contact", "/contactus"]
addToMenu:
  # Legacy weight 1000: top-level, always last.
  - menu: "main-menu"
    order: 1000
  - menu: "footer-menu"
itemsLinkBehavior:
  mode: prefixed
  linkPrefix: linkPrefix
  valueFormatter: phone
---

import ContactForm from "@/components/Form/forms/ContactForm.astro";
import ContentRenderer from "@/components/ContentRenderer/ContentRenderer.astro";
import ContactLeadIn from "@/components/ContactLeadIn.astro";
import BusinessHours from "@/components/BusinessHours.astro";
import Button from "@/components/Button/Button";
import { query } from "@/utils/query";
import { find } from "@/utils/query";
import { businessData, siteData } from "@/content/siteData";

{/* ── /contact-us ────────────────────────────────────────────────────────────
    A deliberately STATIC page rather than a ContactVariant render.

    The inline lead-in row, the hours band over the team photo, and the
    copy-beside-form layout are all page-specific composition — there's
    no second page that wants this shape, so a variant would be indirection
    with one caller. The pieces that ARE reusable stay queried, not hardcoded:
    the contact methods come from the `contact-us` collection, the hours from
    siteData.businessHours, and the socials from `social-media`.

    Adapted from the reference layout, minus the parts Koi doesn't have: no
    mobile app, so no App Store / Google Play / QR-code column. ───────────── */}

{/* Inline lead-in: the prompt, the hours, and the call button on one row.
    Phone comes from the contact-us collection. */}
<ContactLeadIn />

{/* Support hours, from siteData.businessHours — the same array the footer
    prints and the JSON-LD openingHoursSpecification uses. */}
<BusinessHours />

{/* Quote request: copy on the left, the form on the right. */}
<section class="bg-bg text-text py-[var(--spacing-3xl)]">
  <div class="container mx-auto px-4">
    <div class="flex flex-col lg:flex-row gap-10 lg:gap-16 items-start">
      {/* Copy column is the narrower third — the form is the page's purpose,
          so it gets two thirds of the row. */}
      <div class="w-full lg:w-1/3">
        <h2 class="h2 heading-primary">Your home, covered</h2>

        <p class="mt-[var(--spacing-md)] text-lg text-text">
          Tell us about your roof or your energy bills and we'll come back with
          a straight answer — no pressure, no obligation.
        </p>

        <ol class="mt-[var(--spacing-xl)] flex flex-col gap-[var(--spacing-lg)] list-none p-0 m-0">
          <li class="flex gap-[var(--spacing-md)] items-start">
            <span class="flex-shrink-0 w-10 h-10 rounded-full bg-primary text-bg flex items-center justify-center font-bold" aria-hidden="true">1</span>
            <span class="text-lg text-text pt-1">Tell us what your home needs</span>
          </li>
          <li class="flex gap-[var(--spacing-md)] items-start">
            <span class="flex-shrink-0 w-10 h-10 rounded-full bg-primary text-bg flex items-center justify-center font-bold" aria-hidden="true">2</span>
            <span class="text-lg text-text pt-1">Get a free inspection and a clear quote</span>
          </li>
          <li class="flex gap-[var(--spacing-md)] items-start">
            <span class="flex-shrink-0 w-10 h-10 rounded-full bg-primary text-bg flex items-center justify-center font-bold" aria-hidden="true">3</span>
            <span class="text-lg text-text pt-1">
              {businessData.financing} — {businessData.warranty}
            </span>
          </li>
        </ol>

        {/* Socials only. The contact-method cards are gone from here: the
            phone and email already appear in the inline row at the top of the
            page, so repeating them beside the form was the same information
            three times over. */}
        <div class="mt-[var(--spacing-xl)]">
          <ContentRenderer
            query={query("social-media")}
            variant="SocialMediaVariant"
            size="lg"
            alignment="left"
          />
        </div>
      </div>

      <div class="w-full lg:w-2/3 lg:min-w-0">
        <div class="bg-bg-2 rounded-2xl p-[var(--spacing-lg)] md:p-[var(--spacing-xl)] shadow-md">
          <ContactForm />
        </div>
      </div>
    </div>
  </div>
</section>
