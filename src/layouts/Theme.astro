---
// src/layouts/Theme.astro
/**
 * Theme Component - Site Branding and Icons
 * 
 * Handles:
 * - Favicon configuration (multiple sizes and formats)
 * - Apple touch icons
 * - Theme color for mobile browsers
 * - PWA manifest (commented out)
 * 
 * All favicons point to Koi's branded favicon.png.
 * Can be customized to use different images per size.
 */

import "../styles/global.css";
// Scroll/view animations, restored per user request (reverses KOI-REBUILD-PLAN
// §7.9's "no animations" departure). Same library and methodology as
// FariasDemolition and certified-bag-chasers: the JS-observed system, driven by
// `data-animate` attributes on shared components rather than per-page markup.
import "../integrations/scroll-animations/styles/animations.css";
---

<!-- Favicons - multiple sizes for different contexts -->
<!-- Root-relative paths keep icons working on nested routes such as /blog/*
     and /service-areas/*. The prior relative URLs resolved inside each route. -->
<link rel="shortcut icon" type="image/png" href="/favicon.png" />
<link rel="icon" type="image/png" href="/favicon.png" />
<link rel="apple-touch-icon" href="/favicon.png" />

<!-- PWA manifest (uncomment when ready) -->
<!-- <link rel="manifest" href="/site.webmanifest" /> -->

<!-- Mobile browser configuration -->
<meta name="mobile-web-app-capable" content="yes" />

<meta name="theme-color" content="#22324f" />
<!-- Theme color for mobile browsers -->
<!-- <meta name="apple-mobile-web-app-status-bar-style" content="var(--color-primary)" /> -->
<!-- <meta name="theme-color" content="var(--color-primary)" /> -->

<!-- Dynamic theme color from CSS variable (commented out) -->
<!-- 
<script>
  // Set theme color from CSS variable on page load
  document.addEventListener("DOMContentLoaded", () => {
    const rootStyles = getComputedStyle(document.documentElement);
    const primary = rootStyles.getPropertyValue("--color-primary").trim();
    if (primary) {
      document
        .querySelector('meta[name="theme-color"]')
        ?.setAttribute("content", primary);
    }
  });
</script> -->

<!-- Scroll animations (deferred to avoid blocking initial render) -->
<script>
  // Import observer directly to avoid pulling in React via index barrel exports
  import "@/integrations/scroll-animations/observer";
</script>
